import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.eightbitfox.pinkcard',
  appName: 'PinkCard',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
